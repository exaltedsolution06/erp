1)id, menu_title, content_heading, description, image, image_position
---------------------------
1) 'previous_balance' new field - in 'student_session' table.
2) in student_session table 'fees_discount' field set to 0
3) 'previous_balance' new field - in 'receipts' table from collect fee 
4) new table 'previous_balance_paid'


1) Store previous session balance in new field.
2) When Student move to next session then this student ledger balance set to 0 and previous session balance store in field.
3) When collect fee add previous balance field in collect fee page.
4) If any user paid all previous balance then this user previous balance set to 0, otherwise deduct the the pay amount from the previous balance.

//Ticket section
tickets = id, school_code, ticket_type, subject, body, status(0=delete,1=pending,2=open,3=close), created_at, updated_at
ticket_files = id, ticket_id, file
ticket_followup = id, ticket_id, user_type(0=Crm, 1=School), message, created_at
---------------
when pge load - hidden field(original_ledg_bal) = Balance Amt - PREV Balance AMT
		= 3000 - 3000
		= 1000
Currrent ledger update = (Balance Amt - PREV Balance AMT)-hidden field(original_ledg_bal)
			= (3000 - 3000)-1000
			= 0
------------
already check + edit = (Balance Amt - hidden Balance Amt(old_prev_row_balance))+PREV AMT
already check + then uncheck = (hidden_received_discount) + PREV AMT
---------------------------------------------
PREV AMT : Rs. 14000
LEDG AMT : Rs. 200 
Step 1) Collect new fee
   	Select Apr academy
	uncheck PREV AMT
	check LEDG AMT Received all
	From academy 200 bal amount
	Total balance in this slip 200
	Total LEDG AMT is 200

Step 2) Edit this slip
	Uncheck LEDG AMT
	Total LEDG AMT is 3200

when already uncheck LEDG AMT then in edit page need to show minus 

==============
1. Existing Balance Amt = 200
2. Updated Balance = 300
3. Existing LEDG AMT = 0
Existing LEDG AMT + (Updated Balance-xisting Balance Amt)
0+(300-200) = 100

0+(100-200) = -100

200+(200-200) = 200

If balance then plus (main balance - prev balance - original ledger bal)
If discount then minus (main disc - prev disc - original hidden)
$total = balance - disc
